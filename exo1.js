// TO DO : Make a DeepClone Function (use recursion) for 
// - array
// - object
// - date 
// - function
/*When performing a deep copy, those external objects are copied as well, so the new, cloned object is completely independent from the old one.*/

function DeepClone( ) {
    return DeepClone()
}

DeepClone();